interface:
  display_name: "Life"
  short_description: "Repo-specific patterns and workflows for life"
  default_prompt: "Use the life repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true